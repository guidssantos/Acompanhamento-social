import { StyleSheet } from "react-native";

export default StyleSheet.create({
  input: {
    borderRadius: 10,
    borderColor: "white",
    backgroundColor: '#162231',
    borderWidth: 1,
    fontSize: 30

  },
  text:{
        backgroundColor: '#162231'
  }
});