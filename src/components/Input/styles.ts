import { StyleSheet } from "react-native";

export default StyleSheet.create({
  input: {
    borderRadius: 10,
    borderColor: "#33384e",
    backgroundColor: '#FFF',
    fontSize: 30,

  },
  text:{
        backgroundColor: 'transparent',        
  }
});