import { Text } from 'react-native';
import * as Styled from './styles';

export type LoginProps = {title?: string; };
export const Login = ({ title }: LoginProps) => {
return(
<Styled.Wrapper>
    <Text>teste</Text>
</Styled.Wrapper>
)
};
