import { StyleSheet } from "react-native";

export default StyleSheet.create({
  cardContainer: {
    flexDirection: 'row',
    justifyContent: 'center',
    marginTop: 20
  },
  eventCardContainer:{
    marginTop: 20
  }
});